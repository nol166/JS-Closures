// TODO: Create a function that creates a closure. The inner function should log the `startingNum` subtracted from `decreaseBy` on each invocation. See comments on lines 13 - 16.
const countDown = (startingNum, decreaseBy) => {
    // Your code here
}

const countingDown5 = countDown(90, 5)

console.dir(countingDown5()) // Should return 90
console.log(countingDown5()) // Should return 85
console.log(countingDown5()) // Should return 80
console.log(countingDown5()) // Should return 75
